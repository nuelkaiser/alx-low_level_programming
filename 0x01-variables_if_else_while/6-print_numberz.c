#include <stdio.h>
/**
 * main - print all single digit numbers of base
 * 10 stating from 0, followed by a new line.
 * Return Always 0 (Success)
 */
int main(void)
{
int a;
for (a = 48; a < 58; a++)
{
putchar(a);
}
putchar("\n");
return (0);
}
