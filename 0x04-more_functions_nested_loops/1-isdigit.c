#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @digit: int to be checked
 * Return: 1 if num is a digit, 0 otherwise
 */
int _isdigit(int digit)
{
	return (digit >= '0' && digit <= '9');
}
